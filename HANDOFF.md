# 引き継ぎ資料（証券管理ツール）

このドキュメントは、開発をローカル環境に移して継続するための引き継ぎ資料です。
「①ローカルへの取り込み → ②ツールの意図・仕様 → ③実装状況 → ④残作業」の順でまとめています。

最終更新: 2026-06-01

---

## 次セッションへの引継ぎ（2026-06-01・最優先で読む）

> このセッションは、Claude側のツール呼び出しが断続的に壊れた（送信が生テキスト化）ため、別セッションに切替。
> **コードはすべてコミット・push済み（作業ツリーはクリーン）**。最新コミット `891b5b1`。
> ブランチ `claude/securities-portfolio-tool-WGIsF`（＝本番。push で CF Pages 自動デプロイ）。

### 現在の状態
- 状態: **ユーザー確認待ち**（下記「いま止まっている論点」）＋ Google/CF は準備待ち
- ボール: すみぽん（①エクスポート方針の回答 ②Google Cloud のクライアントID）
- ローカルサーバー: `npx wrangler pages dev . --port 8788 --compatibility-date=2026-04-27` が**起動中の可能性**（http://localhost:8788）。落ちていたら再起動。

### いま止まっている論点（最優先・着手前に確認）
**タスク: 「資産管理エクセル貼付用エクスポート」（Notion 議論中・ボール=すみぽん）**
- 目的: 『証券会社データ変換用.xlsm』の出力シート（日本株/米国株・12列）を本ツールから出力し、変換エクセル（マネフォ以外）を置き換える。
- 出力列(12): 銘柄/コード/種別/詳細種別/証券会社/証券区分等/通貨/評価円/評価ドル/取得円/取得ドル/積立。
- 確定済みの方針: **出力単位＝シート別（日本株/米国株）**、**対象＝取込した証券会社データ（手入力除外）**、**取込バッチ(ファイル)にチェックを付けて選択**。投信・現金/銀行(マネフォ)・積立は対象外。
- **未回答の質問（A/B/C）= 米国株/外貨取得の「取得円」の出し方**:
  - A: 取引/保有入力に「取得額(円)」任意欄を追加し出力に使う（内部判定には未使用）= 正確。※Claude推奨
  - B: 取得ドル×現在為替で近似（不正確）
  - C: 米国株は取得ドルのみ出力（取得円は空欄）
  → **すみぽんの回答を得てから実装すること。**
- 実装前の要確認: 「取込バッチのチェック選択」には、保有↔取込バッチの紐づけが必要。`store.data.importHistory` の構造（どの銘柄/保有が含まれるか）を確認してから設計する。
- 参照: `reference/証券会社データ変換用.xlsm`（.gitignore除外）、`reference/_xlsm_dump.txt`（出力シート構成のダンプ）。

### Google/CF フェーズ（準備待ち・対応中）
- **Googleログイン＋Sheets保存**: 方式A(ブラウザ完結GIS)で**休眠スキャフォールド実装済み**（commit 8de1816、DESIGN.md §14、マスタ・設定の「Google連携（実験的）」）。clientId 未設定なら現行アプリに無影響。
- すみぽん準備: Google Cloud で Sheets API 有効化＋ウェブ用 OAuthクライアントID 作成（JS生成元に秘匿CF URL＋localhost）→ **クライアントIDを共有**（公開情報・安全、シークレット不要）。受領後にマスタ・設定へ入力し、ログイン→保存(_appdata!A1にJSON)→別端末で読込を実機確認。
- 他: 定時通知(LINE+Resend)、Cron、KV、日本株ファンダ本番検証 は未着手（要外部準備）。

### 直近セッションで完了した主な作業（すべて push 済み）
- 前日比バグ修正(日足化 SEC-42)、通貨記号除去/表示桁(SEC-44/45)、残り下落率マイナス(SEC-38)、条件付き背景色(SEC-41)、残り下落率(前日)(SEC-52)、サインタブ刷新(SEC-46)、ライトテーマ(SEC-47)、分割で価格消失修正(SEC-48)、JP小数表示(SEC-49)、銘柄詳細＋チャート(SEC-15)、銘柄マスタタブ(SEC-27)、買増固定値(SEC-39)、買い増しルール列(SEC-40)、コード/銘柄名クリックで詳細、前日比/株価クリックで株探、レポートタブ(SEC-17)、ダッシュボード前日比＋参考指数、ソートで列幅不変、銘柄マスタソート＋削除前バックアップ。
- ドキュメント現状化（DESIGN §0/§2.5/§14、REQUIREMENTS、HANDOFF）。

### 検証について
- 各実装はローカルプレビュー（静的 8123 / wrangler 8788）でeval検証済み。**実機（実データ）での最終確認はすみぽん**。
- 未確認: Google連携の実OAuth/Sheets動作（クライアントID未入手）。日本株ファンダの本番取得可否（CF本番未検証）。

### 次セッションの再開手順
1. `CLAUDE.md`（タスクDB運用ルール）と本 `HANDOFF.md`（特に本セクション）を読む。
2. Notion タスクDB「証券管理ツールPJタスク」でボール=Claude かつ ステータス≠完了/不要 を確認。
3. `git pull`、`git log --oneline -8` で最新差分を把握。
4. **エクスポート機能**は、すみぽんの「取得円A/B/C」回答を得てから着手（未回答なら催促）。`reference/_xlsm_dump.txt` で出力仕様を確認、`store.data.importHistory` の構造を確認。
5. **Google連携**は、クライアントID受領後にマスタ・設定で実機確認。
6. ローカル起動: `npx wrangler pages dev . --port 8788 --compatibility-date=2026-04-27`。

### やってはいけないこと
- すみぽん回答前にエクスポートの「取得円」方式を勝手に決めて実装しない。
- バグ/タスクを実機OKなしに「完了」へ繰り上げない（バグは push 後も「対応中」維持＝CLAUDE.md ルール）。
- `reference/` の個人データ（xlsx/xlsm）をコミットしない（.gitignore済み）。
- 秘匿CFのURL・クライアントシークレット等をコミット/チャットに残さない。

---

## 0. まず最初に：ローカルへの取り込み手順

コードはすべて GitHub のブランチ **`claude/securities-portfolio-tool-WGIsF`** に push 済みです。
ローカルに取り込んでから作業を始めてください。

### 初めて取得する場合
```bash
git clone https://github.com/ziqebPmH473/securities-manager.git
cd securities-manager
git checkout claude/securities-portfolio-tool-WGIsF
```

### すでにクローン済みの場合（最新を取得）
```bash
cd securities-manager
git fetch origin
git checkout claude/securities-portfolio-tool-WGIsF
git pull origin claude/securities-portfolio-tool-WGIsF
```

### 主なファイル一覧
| ファイル | 役割 |
|----------|------|
| `index.html` | 画面の骨組み（タブUI・モーダル）。タブ: ダッシュボード/米国株/日本株/サイン/分割/レポート/銘柄マスタ/マスタ・設定 |
| `styles.css` | スタイル（**ライトテーマ＝白ベース**・レスポンシブ）。色は `:root` のCSS変数に集約 |
| `app.js` | アプリ本体（データ`store`・判定`calc`・描画`render*`・フォーム`open*`） |
| `functions/api/price.js` | 価格取得プロキシ（Yahoo中継。**日足取得で前日比を正常化**） |
| `functions/api/info.js` | 銘柄情報取得（日本語名・セクター/業種・EPS/発行済株式数等） |
| `functions/api/splits.js` | 株式分割・併合の取得（Yahoo `events.splits`） |
| `functions/api/history.js` | 価格履歴（終値時系列。銘柄詳細チャート用） |
| `reference/` | 参照用の個人データ（`米国株管理.xlsx` 等）。**.gitignoreで除外**（コミットされない） |
| `README.md` / `REQUIREMENTS.md` / `DESIGN.md` | 動かし方 / 要件 / 詳細設計 |

> 以後の更新は `git pull` で取り込めます。手動コピーは不要です。
> ブランチ `claude/securities-portfolio-tool-WGIsF` が既定（本番）ブランチで、**push すると CF Pages が自動デプロイ**します（`main` は無し）。

### ローカルでの起動（自分のPCなので localhost が確実に開きます）
```bash
# A. 価格自動取得つき（推奨・最終形に近い）
npx wrangler pages dev .
#   → http://localhost:8788

# B. 静的サーバー（価格は手入力）
python -m http.server 8000   # → http://localhost:8000
#   または npx serve .

# C. もっと簡単に：index.html をダブルクリックで直接開く（価格は手入力）
```

---

## 1. このツールの目的・意図

保有株（**日本株・米国株・投資信託**）の資産状況を把握し、**銘柄ごとの買い増しルールに基づく
「買い時（買い増しサイン）」を逃さない**ことが最重要目的。スマホ・PC両方から同じデータを見たい。

### 最重要要件：買い増しルール
- **初回購入トリガー**: 基準高値（デフォルト=**5年高値**）から **−40%** 下落で「初回購入サイン」
- **買い増しトリガー**: **前回購入価格** から **−20%** 下落で「買い増しサイン」
- 下落率・基準高値モードは**ルールマスタ**で変更可能（デフォルトは −40% / −20% / 5年高値）
- 一覧・サインに「**買い増しまであと何%（通知単価まで）**」を表示

### 買い増し金額：カテゴリ別マスタ（価格非依存の固定値）
銘柄の「カテゴリ（格・成長確度）」で1回の推奨買い増し額が決まる。**米国株は ÷100 したドル**。

| カテゴリ | 位置づけ | 日本株(円) | 米国株($) |
|---|---|--:|--:|
| 王道・鉄板 | 文明のインフラ | 80,000 | 800 |
| 主力・成長 | 世界的覇権 | 60,000 | 600 |
| 準主力 | 地域覇者・ニッチ | 50,000 | 500 |
| 防御・配当 | 成熟・安定 | 40,000 | 400 |
| 有望な投機 | 宝くじのエース | 25,000 | 250 |
| お遊び | 記念・優待 | 15,000 | 150 |
| 対象外 | 投資不適格 | 0 | 0 |

> 金額は株価の割安/割高で増減させない固定値（「鉄の掟」: 80kは迷えば60k／一国限定は最大50k／
> キャピタルゲイン見込み薄は40k以下）。カテゴリ判定は人の判断、アプリは割当結果と金額を保持。

### その他の確定仕様（詳細は REQUIREMENTS.md / DESIGN.md）
- **市場を混ぜない**: 米国株/日本株/投信をタブで分離。ダッシュボードは合算＋市場別内訳
- **売却**: 数量のみ減算・**平均取得単価は不変**。買いは加重平均で単価更新
- **取引区分**: 証券会社（SBI/楽天/Webull/moomoo）×口座（特定/NISA）、端株（小数）対応
- **資産クラス**: 投信も資産表示に含めるが、**買い増し判定は個別株のみ**
- **株価**: 米株=ほぼリアルタイム(Finnhub・将来) / 日本株=15〜20分遅延(Yahoo) / 投信=日次基準価額
- **通知（将来）**: 定時バッチ。日本株 7:45/11:00/17:00、米国株 24:00/7:00（JST）。LINE＋メール(Resend)
- **保管先（将来）**: 自分の **Google スプレッドシート**（資産データ）＋価格はアプリ側キャッシュ
- **認証（将来）**: **Googleログイン**（許可アカウント限定）。データはGoogle側に残りホスト非依存

---

## 2. アーキテクチャ（最終形）

```
[ブラウザ] ─ Googleログイン ─▶ [Cloudflare Pages / Functions]
                                   ├─▶ Google スプレッドシート（資産データの原本）
                                   └─▶ Cloudflare KV（価格・為替キャッシュ＝一時データ）
                          [Cron] ──┤   ├─ Finnhub(米株) / Yahoo(日株・為替)
                                   └─  └─ LINE / Resend（定時通知）
```
- 価格などの頻繁更新は KV、永続資産データは Sheets に分離（Sheets API 制限を回避・無料・データ所有）
- Sheets への書き込みはバックエンドに一本化（read-modify-write、単一ユーザー前提）

---

## 3. 現在の実装状況（2026-06-01時点）

バニラ HTML/CSS/JS（ビルドなし）＋ Cloudflare Pages Functions。保存は localStorage。
当初設計（§2 の Sheets/Cron/通知）の多くは**未実装＝次フェーズ**で、現状はクライアント完結。

### 実装済み（主な機能）
- **ポートフォリオ**: 銘柄登録（市場/カテゴリ/ルール/判定有効無効/注意フラグ/前回購入価格/**買増固定値**/基準高値モード/**名前・セクター・業種の手動上書き**）、取引手入力（買=加重平均・売=数量減算）、証券会社×口座、端株、全口座合算
- **価格・情報取得**: `/api/price`（Yahoo・**日足取得で前日比を正常化**=SEC-42）、`/api/info`（日本語名・セクター/業種・EPS/発行済株式数）、`/api/history`（終値時系列）、`/api/splits`（分割）。USD/JPY 円換算。名前/ファンダは meta マスタにキャッシュ
- **買い増し判定**: 初回−40%/買い増し−20%・5年高値基準（ルールで可変）、前回購入が無ければ取得単価を「みなし(≒)」、**買増固定値があれば固定トリガー(固)**。残り下落率を**現在値ベース＋前日終値ベース(前日)**の2列、到達はマイナス表示
- **サインタブ**: 到達/もうすぐを1表に統合（列幅統一）、**市場フィルタ(全/日/米)**、**証券会社列**（最後に購入した会社）
- **株式分割・併合**: 検知→承認→調整（推奨処理列・一括調整モーダル・現→後プレビュー）。**価格キャッシュは保持**し手入力項目/保有/取引のみ調整（SEC-48）
- **一覧UI**: 列の表示非表示/並び替え（列ピッカー）、**見出し固定（枠内スクロール）**、**条件付き背景色**（前日比・5年高値下落率・前回からの下落率／nice刻み）、**通貨記号なし・表示桁ルール**（米2桁/日整数・数量 米5桁/日整数）、**コード/銘柄名クリックで詳細**、**前日比/株価クリックで株探チャート(新タブ)**、表示は「保有 or 注意銘柄」のみ
- **銘柄詳細モーダル**: 価格チャート(5年・終値、トリガー/現在値/前回購入線・高安・補助目盛)＋判定/保有/取引履歴/分析メタ/ファンダ
- **銘柄マスタタブ**: 全銘柄の固有データ参照・編集（売却済みも保管）
- **ダッシュボード**: 総括カード、市場別内訳、**前日比（金額・円換算 日/米/全体）＋参考指数**（日経/TOPIX(1306.T)/S&P500/NASDAQ100）
- **レポートタブ**: 市場別/証券会社別/証券会社×市場/取引サマリーの集計（円換算）
- **マスタ・設定**: カテゴリ別金額マスタ（版管理・履歴）、ルール編集、汎用CSV取込（証券会社別マッピング・洗い替え・履歴）、エクスポート/インポート(JSON)、全データ削除
- **ライトテーマ（白ベース）**

### 現在の制約（次フェーズで解消）
- **ログインなし** / **保存先=localStorage**（Google ログイン＋Sheets保存は次フェーズ）
- **通知なし** / **Cron なし** / **KV なし**
- 米株も Yahoo 経由（Finnhub未使用。`.dev.vars`/CF env に `FINNHUB_API_KEY` 設定で切替可）
- **投信(FUND)はツールから除外**（2026-05-30判断。NAV自動取得の無料汎用APIが無いため）
- 日本株ファンダ（配当/PER/時価総額）はローカル wrangler では quoteSummary がブロックされ取得不可。**本番(CF)で要検証**

### コードの読みどころ（app.js）
- `store` … localStorage永続化(`sm_data_v1`)・CRUD・取引反映・`applySplit`
- `calc` … 判定(`evaluate`)・基準高値・残り下落率(`remainingDropPrev`)・円換算(`toJpy`)・前日比(`dayChangeJpy`)・指数(`indexChangePct`)・`lastBroker`
- `MASTER_COLS`/`COL_RENDERERS`/`getColOrder` … 列定義・描画・列ピッカー
- `CF_RULES`/`condStyle` … 条件付き背景色、`fmtAmt`/`fmtQty` … 表示桁
- `api.refreshAll` … 価格・為替・指数(`INDICES`)を取得しキャッシュ
- `render*` … dashboard / market / signals / splitsTab / report / secMaster / master
- `openSecurityDetail` … 銘柄詳細＋`detailSvgChart`（バニラSVG）

---

## 4. 残作業・次フェーズ（推奨順）

**残りは実質 Google/CF・通知系インフラのみ**（フロント機能はほぼ出揃い）。

1. **Googleログイン＋Googleスプレッドシート保存**（基盤）。方式は **A: ブラウザ完結（GIS）** で確定。
   - 準備（すみぽん）: Google Cloud で **Sheets API 有効化 ＋ ウェブ用 OAuthクライアントID 作成**、
     承認済みJS生成元に「秘匿CFのURL」＋`http://localhost:8788` を登録 → **クライアントIDを共有**（公開情報・安全。シークレットは方式Aでは不要）
   - 保存形式: v1は **JSONブロブ（1タブに丸ごと保存）** 推奨、将来 表形式へ
2. **定時通知**（LINE Messaging API ＋ Resend メール）。要: LINE公式アカウント、CF env にトークン
3. **Cron Triggers**（定時ポーリング→判定→サイン記録→定時通知）／ **価格キャッシュをKVへ**
4. 日本株ファンダの**本番検証・定期取得**、米株 Finnhub 準リアルタイム
5. レポートの**資産推移グラフ／サイン履歴**（日次スナップショット保存＝Sheets移行後）

### 未確定・要準備
- ログイン許可する Google アカウント（＝Sheets を置くアカウントと同一でOK）
- LINE公式アカウント（Messaging API チャネル）
- 秘匿CFの本番URL（OAuth生成元に登録。チャットには貼らない運用）

---

## 5. 動作確認の方法（参考）

ローカルにブラウザ自動テスト（Playwright）を入れれば、回帰確認ができます。
MVPでは以下の流れを検証済み：
- 起動時にモーダル非表示 → 銘柄追加 → 価格入力（現値120/5年高値200）→ 初回トリガー120で「到達」
- サイン一覧に推奨額 $600（主力・成長 60000÷100）→ 購入記録（2株@120）→ addon基準120・
  トリガー96で「あと20%」に遷移 → ダッシュボード集計

---

## 6. 参照ドキュメント
- `REQUIREMENTS.md` … 要件定義（確定）
- `DESIGN.md` … 詳細設計（スキーマ・判定ロジック・API・通知・Sheets構成）
- `README.md` … 動かし方・現状スコープ
