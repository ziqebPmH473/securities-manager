# 引き継ぎ資料（証券管理ツール）

このドキュメントは、開発をローカル環境に移して継続するための引き継ぎ資料です。
「①ローカルへの取り込み → ②ツールの意図・仕様 → ③実装状況 → ④残作業」の順でまとめています。

最終更新: 2026-05-29

---

## 0. まず最初に：ローカルへの取り込み手順

コードはすべて GitHub のブランチ **`claude/securities-portfolio-tool-WGIsF`** に push 済みです。
ローカルに取り込んでから作業を始めてください。

### 初めて取得する場合
```bash
git clone https://github.com/ziqebPmH473/securities-manager.git
cd securities-manager
git checkout claude/securities-portfolio-tool-WGIsF
```

### すでにクローン済みの場合（最新を取得）
```bash
cd securities-manager
git fetch origin
git checkout claude/securities-portfolio-tool-WGIsF
git pull origin claude/securities-portfolio-tool-WGIsF
```

### 取得されるファイル一覧（全7ファイル）
| ファイル | 役割 |
|----------|------|
| `index.html` | 画面の骨組み（タブUI・モーダル） |
| `styles.css` | スタイル（ダークテーマ・レスポンシブ） |
| `app.js` | アプリ本体（データ・判定ロジック・描画・フォーム） |
| `functions/api/price.js` | 価格取得プロキシ（Cloudflare Pages Function。Yahoo中継） |
| `README.md` | 動かし方・現状スコープ |
| `REQUIREMENTS.md` | 要件定義書（確定済み） |
| `DESIGN.md` | 詳細設計書（確定済み） |

> 以後の更新は `git pull` で取り込めます。手動コピーは不要です。

### ローカルでの起動（自分のPCなので localhost が確実に開きます）
```bash
# A. 価格自動取得つき（推奨・最終形に近い）
npx wrangler pages dev .
#   → http://localhost:8788

# B. 静的サーバー（価格は手入力）
python -m http.server 8000   # → http://localhost:8000
#   または npx serve .

# C. もっと簡単に：index.html をダブルクリックで直接開く（価格は手入力）
```

---

## 1. このツールの目的・意図

保有株（**日本株・米国株・投資信託**）の資産状況を把握し、**銘柄ごとの買い増しルールに基づく
「買い時（買い増しサイン）」を逃さない**ことが最重要目的。スマホ・PC両方から同じデータを見たい。

### 最重要要件：買い増しルール
- **初回購入トリガー**: 基準高値（デフォルト=**5年高値**）から **−40%** 下落で「初回購入サイン」
- **買い増しトリガー**: **前回購入価格** から **−20%** 下落で「買い増しサイン」
- 下落率・基準高値モードは**ルールマスタ**で変更可能（デフォルトは −40% / −20% / 5年高値）
- 一覧・サインに「**買い増しまであと何%（通知単価まで）**」を表示

### 買い増し金額：カテゴリ別マスタ（価格非依存の固定値）
銘柄の「カテゴリ（格・成長確度）」で1回の推奨買い増し額が決まる。**米国株は ÷100 したドル**。

| カテゴリ | 位置づけ | 日本株(円) | 米国株($) |
|---|---|--:|--:|
| 王道・鉄板 | 文明のインフラ | 80,000 | 800 |
| 主力・成長 | 世界的覇権 | 60,000 | 600 |
| 準主力 | 地域覇者・ニッチ | 50,000 | 500 |
| 防御・配当 | 成熟・安定 | 40,000 | 400 |
| 有望な投機 | 宝くじのエース | 25,000 | 250 |
| お遊び | 記念・優待 | 15,000 | 150 |
| 対象外 | 投資不適格 | 0 | 0 |

> 金額は株価の割安/割高で増減させない固定値（「鉄の掟」: 80kは迷えば60k／一国限定は最大50k／
> キャピタルゲイン見込み薄は40k以下）。カテゴリ判定は人の判断、アプリは割当結果と金額を保持。

### その他の確定仕様（詳細は REQUIREMENTS.md / DESIGN.md）
- **市場を混ぜない**: 米国株/日本株/投信をタブで分離。ダッシュボードは合算＋市場別内訳
- **売却**: 数量のみ減算・**平均取得単価は不変**。買いは加重平均で単価更新
- **取引区分**: 証券会社（SBI/楽天/Webull/moomoo）×口座（特定/NISA）、端株（小数）対応
- **資産クラス**: 投信も資産表示に含めるが、**買い増し判定は個別株のみ**
- **株価**: 米株=ほぼリアルタイム(Finnhub・将来) / 日本株=15〜20分遅延(Yahoo) / 投信=日次基準価額
- **通知（将来）**: 定時バッチ。日本株 7:45/11:00/17:00、米国株 24:00/7:00（JST）。LINE＋メール(Resend)
- **保管先（将来）**: 自分の **Google スプレッドシート**（資産データ）＋価格はアプリ側キャッシュ
- **認証（将来）**: **Googleログイン**（許可アカウント限定）。データはGoogle側に残りホスト非依存

---

## 2. アーキテクチャ（最終形）

```
[ブラウザ] ─ Googleログイン ─▶ [Cloudflare Pages / Functions]
                                   ├─▶ Google スプレッドシート（資産データの原本）
                                   └─▶ Cloudflare KV（価格・為替キャッシュ＝一時データ）
                          [Cron] ──┤   ├─ Finnhub(米株) / Yahoo(日株・為替)
                                   └─  └─ LINE / Resend（定時通知）
```
- 価格などの頻繁更新は KV、永続資産データは Sheets に分離（Sheets API 制限を回避・無料・データ所有）
- Sheets への書き込みはバックエンドに一本化（read-modify-write、単一ユーザー前提）

---

## 3. 現在の実装状況（MVP・初版）

「まず動かして確認できること」を優先した初版。**実機テスト済み（Playwrightで全13項目PASS）**。

### 実装済み
- 銘柄登録（市場・カテゴリ・判定有効/無効・前回購入価格の手動指定）
- 取引の手入力（買い=数量加算＆加重平均で単価更新 / 売り=数量のみ減算）
- 証券会社×口座種別、端株対応、銘柄単位での全口座合算
- 価格の自動取得（`/api/price`→Yahoo）＋手入力フォールバック、USD/JPY 円換算
- 買い増し判定（初回−40%/買い増し−20%、5年高値基準）、「あと何%」表示、サイン一覧
- カテゴリ別金額マスタ（編集可・米株÷100ドル）、ルール編集
- 市場タブ分離、ダッシュボード（円換算・市場別内訳）
- データのエクスポート/インポート(JSON)

### 現在の制約（MVPゆえの割り切り。最終形では解消予定）
- **ログインなし**（確認を簡単にするため。後でGoogleログイン追加）
- **保存先 = ブラウザの localStorage**（後で Google スプレッドシートへ移行）
- **通知なし**（LINE公式アカウント未準備のため。今はアプリ内サイン一覧で確認）
- **米株もYahoo経由（15〜20分遅延）**。Finnhubによる準リアルタイムは未実装
- **投信の基準価額自動取得は未実装**（手入力で資産表示は可能）
- 価格チャート・CSV取込は未実装

### データ構造（localStorage キー: `sm_data_v1`）
`app.js` の `store` 参照。`securities / holdings / transactions / rules / categories /
prices / fx / seq`。将来 Sheets の各タブへ 1:1 で移行しやすい形にしてある。

### コードの読みどころ（app.js）
- `store` … localStorage 永続化・CRUD・取引の保有反映（買い=加重平均/売り=数量のみ）
- `calc` … 買い増し判定（`evaluate`）、基準高値、円換算評価額
- `api.refreshAll` … `/api/price` から価格・為替を取得しキャッシュ更新
- `render*` … 画面描画（dashboard / market / signals / master）
- `open*Form` … 各モーダル（銘柄・取引・価格入力・カテゴリ・ルール）

---

## 4. 残作業・次フェーズ（推奨順）

1. **Cloudflare Pages へデプロイ**（公開URLでスマホ/PCから確認）
2. **保管先を Google スプレッドシートへ**（localStorage → Sheets API。バックエンド集約）
3. **Googleログイン**（許可アカウント＝本人のGoogleに限定）
4. **定時通知**（LINE公式アカウント準備後 → Messaging API ＋ Resendメール）
5. 米株ほぼリアルタイム（Finnhub APIキー）、投信の日次基準価額取得
6. 価格チャート＋トリガーライン、CSV取込（SBI/楽天/Webull/moomoo、サンプル入手後）

### 未確定・要準備（実装着手前に）
- LINE公式アカウント（Messaging API チャネル）の発行
- ログイン許可する Google アカウント（＝Sheets を置くアカウントと同一でOK）
- 各証券会社の実CSVサンプル（CSV取込フェーズで使用）

---

## 5. 動作確認の方法（参考）

ローカルにブラウザ自動テスト（Playwright）を入れれば、回帰確認ができます。
MVPでは以下の流れを検証済み：
- 起動時にモーダル非表示 → 銘柄追加 → 価格入力（現値120/5年高値200）→ 初回トリガー120で「到達」
- サイン一覧に推奨額 $600（主力・成長 60000÷100）→ 購入記録（2株@120）→ addon基準120・
  トリガー96で「あと20%」に遷移 → ダッシュボード集計

---

## 6. 参照ドキュメント
- `REQUIREMENTS.md` … 要件定義（確定）
- `DESIGN.md` … 詳細設計（スキーマ・判定ロジック・API・通知・Sheets構成）
- `README.md` … 動かし方・現状スコープ
