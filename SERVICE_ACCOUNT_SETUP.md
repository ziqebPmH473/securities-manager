# Google サービスアカウント作成手順（通知 N1 の前提）

> 通知機能の土台（N1：サーバーがデータを読む）に必要な、すみぽん側の準備手順です。
> 関連: `NOTIFICATION_PLAN.md` / Notion「📋 通知機能 実装計画・実施順（SEC-98）」

## これは何をするの？（噛み砕き）

- **サービスアカウント** = 人が使わない「**プログラム専用のGoogleユーザー（ロボット用アカウント）**」。
- 通知は「ツールを開いていない時」にもサーバー（Cloudflare）が裏で価格を見て判定する必要があります。
  そのサーバーが、データの入った**Googleスプレッドシートを読む**ために、このロボット用アカウントを使います。
- **鍵(JSON)** = そのロボットの「パスワード代わり」。サーバーだけが持ちます（秘密）。
- **共有** = ロボットに「このシートを見ていいよ」と許可する操作。普通のファイル共有と同じです。

前提：ブラウザの「Google連携」でスプレッドシート（`_appdata` シート）への保存/読込がすでに動いていること。

---

## 手順

### 1. Google Cloud プロジェクトを用意
1. https://console.cloud.google.com/ を開く
2. 画面上部のプロジェクト選択 →「**新しいプロジェクト**」→ 名前（例: `securities-manager`）→「作成」
   - 既存プロジェクトがあればそれを使ってもOK

### 2. Google Sheets API を有効化
1. 左メニュー「**APIとサービス**」→「**ライブラリ**」
2. `Google Sheets API` を検索 → 開く →「**有効にする**」

### 3. サービスアカウントを作成
1. 「APIとサービス」→「**認証情報**」→「**＋認証情報を作成**」→「**サービスアカウント**」
2. 名前（例: `sheets-reader`）→「作成して続行」
3. ロール（権限）は**未設定のままでOK**（権限はシート共有で渡すため）→「完了」
4. 作成されたサービスアカウントの **メールアドレス** を控える
   - 例: `sheets-reader@securities-manager.iam.gserviceaccount.com`

### 4. 鍵（JSON）を発行
1. 作ったサービスアカウントをクリック →「**キー**」タブ
2. 「鍵を追加」→「**新しい鍵を作成**」→ 形式「**JSON**」→「作成」
3. JSONファイルが自動ダウンロードされます
   - ⚠️ **このファイルは秘密**です。GitHub等に上げない・人に見せない。

### 5. スプレッドシートをサービスアカウントに共有
1. データの入っている**スプレッドシートを開く**
2. 右上「**共有**」→ 手順3で控えた**サービスアカウントのメールアドレス**を入力
3. 権限「**閲覧者**」（読むだけならこれでOK。将来サーバーからも書くなら「編集者」）→「送信」

### 6. Claude に渡すもの
- サービスアカウントの **JSON鍵の中身**（特に `client_email` と `private_key`）
- 対象**スプレッドシートのID**（URLの `/d/` と `/edit` の間の文字列）

> 渡し方：`private_key` は改行を含むため、チャット直貼りだと壊れやすいです。安全な渡し方（一時的な置き場所や貼り方）は別途ご案内します。受け取ったら Claude が Cloudflare の **Secrets（秘密の環境変数）** に登録し、サーバーから `_appdata` を読めるか検証します（= N1）。

---

## このあとの流れ
1. （すみぽん）上記＋**通知チャネルの決定**（推奨初手：メール=Resend）
2. （Claude）N1：Cloudflare Secrets 登録 → サーバーから `_appdata` 読取検証
3. 以降 N2（判定のサーバー移植）→ N3（通知送信）→ N4（定時バッチ）
