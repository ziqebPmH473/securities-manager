# 時価総額補正（補正係数・補正推奨額）実装計画

起票: 2026-09-18（すみぽん × Claude 合意済みの仕様）
関連タスク: Notion「時価総額補正（補正係数・補正推奨額）」／別タスク: 取引登録画面の情報帯・ダッシュボード銘柄名→カルテ・外国ADRの時価総額を為替換算

---

## 1. 目的

- 安全性の補正ではない。企業の質は従来どおり 格付・カテゴリ（基本推奨額）で評価する。
- **巨大企業は100倍が期待しにくいので元本を厚くして利益寄与を上げる**。小型株は倍率が効くので増やさない。
  - 小型株＝倍率を働かせる／大型株＝元本を働かせる
- 係数は「その買いサイクル開始時点の倍率余地補正」。基本推奨額は「現在の企業の格」。

## 2. 計算

| 記号 | 意味 | 出典 |
|---|---|---|
| 基本額 | 1回の購入額。手入力 `sec.buyAmount` 優先、無ければカテゴリ金額 | 既存 `calc.buyAmount(sec)` |
| D | 市場（日米別）時価総額1位 ÷ 自分の時価総額 | 既存 `calc.capToTop(sec)` |
| M | 1位にかける倍数（マスタ・初期 2.0） | 新設定 |
| T | 係数をかけ始める1位までの倍率（マスタ・初期 200） | 新設定 |

- **係数** = 1 + (M−1) × log(T/D) ÷ log(T)。D ≧ T → 1.0、D ≦ 1 → M。**係数の刻み（マスタ・初期 0.05。0.1 等に変更可）で切り捨て**。
  - 浮動小数の誤差対策: 切り捨ては `Math.floor(x / step + 1e-9) * step` とし、表示は刻みの桁数で丸める。
- **補正推奨額** = 基本額 × 係数 を 丸め単位で**四捨五入**（JP: 5,000円／US: $50。マスタで変更可）。
- 係数 1.00 の時（条件外・D未取得を含む）は丸めず **基本額そのまま**。
- 検算: 60,000×1.35=81,000→80,000／80,000×1.35=108,000→110,000。D=100→1.13→1.10。

## 3. 適用条件（マスタ）

- 格付「〇以上」＋カテゴリ「〇以上」＋つなぎ「かつ／または」。**初期値: B以上 かつ 準主力以上**。
- 格付の順位は `GRADE_RANK`（S>A>B>C>D）。格付は `sec.rating`。
- カテゴリの「以上」はカテゴリマスタの `sortOrder`（小さいほど上位）。条件はカテゴリ名で保持し、`renameCategory` で追従、削除時は条件を「未設定（カテゴリ条件なし）」に戻す。
- 条件外は係数 1.00。

## 4. マスタ設定（`store.data.settings`・keyedTs 同期）

`settings.capCoef = { maxMult: 2.0, startRatio: 200, coefStep: 0.05, gradeMin: 'B', catMin: '準主力', join: 'and', roundJpy: 5000, roundUsd: 50 }`
- `coefStep`（係数の刻み）は 0.01 以上の正の数。0.05／0.1 などを想定。
- 保存時 `settings._updatedAt = store._now()`（ルール7）。
- 画面: マスタ（設定）に「時価総額補正」セクション。変更時の副作用は §6.4・§6.5。

## 5. 列（既存の項目は上書きしない）

- `capCoef`「補正係数」: 係数（固定中は 🔒、ツールチップに D・固定日・きっかけ）。
- `capAmount`「補正推奨額」: 市場通貨で表示。
- 既定表示: **保有銘柄（US/JP）と買い増しサイン（SIGNAL）**。
- ルール5 配線: `MASTER_COLS`／`COL_RENDERERS`／`DEFAULT_VISIBLE`／`getSortVal`・`sortValue`／`colDefaultWidth`。
  派生値なので **フォーム・取込（GI/汎用往復）・一括変更は対象外**（理由: 計算値／固定は履歴と連動し外部から ON/OFF すると整合が崩れる。すみぽん了承済み）。
  転記/エクスポートは列定義に追従する箇所のみ確認。分割調整は D が比率なので不要（基本額は分割で変わらない）。

## 6. 固定（サイクル）

### 6.1 状態
- **変動中**: 表示のたびに最新の D・現在のマスタ・現在の条件で計算。
- **固定中**: 固定時の **D と 条件判定結果** を保持。株価が動いても変わらない。

### 6.2 固定する（チェックが付く）
1. 変動中の銘柄で**買い取引を登録**した時（手入力・CSV取込とも。係数は登録時点の時価総額で算出）。
   - **条件外でも固定**（係数 1.00 で固定。そのサイクルは 1.00 のまま）。
   - D 未取得なら固定保留 → D が取れた最初のタイミングで固定。
2. **導入時の移行**（1回だけ）: 買い取引が登録済みの銘柄を、導入時点の D で固定。固定日＝最後の買い取引日。
   ただし最後の買いより後に高値更新済みなら変動中。買い取引が無い銘柄は変動中。
3. **手動でチェックを付けた**時（確認ダイアログ。D 未取得なら「時価総額が未取得のため固定できません」で不可）。

### 6.3 固定を外す（チェックが外れる）
1. **高値更新**（自動）: 買い増しルールと同じ基準高値（`calc.baseHigh`／`baseHighDate`、ルールの 5y/52w/all）で
   「基準高値が付いた日 ＞ 固定日」。`rule.highResetMode` の ON/OFF は見ない。
   固定日＝買いによる固定なら取引日、手動固定なら手動固定日。基準高値が手動（日付なし）の銘柄は自動解除しない。
2. **手動でチェックを外す**（確認ダイアログ）。
3. **固定のきっかけの買い取引を削除** → その固定レコードを取り消し1つ前の状態に戻す。
4. **固定中の銘柄が条件から外れた**時（取込・銘柄編集・マスタの条件変更）:
   対象銘柄を一覧にした**確認画面を1回**出す。行ごとに「固定を外す」チェック（初期は全部ON）。
   各行に「現在: 推奨額 80,000（係数1.35で固定）→ 外した後: 60,000（係数1.00・対象外）」。外さなかった銘柄は固定継続。
- 条件を**満たすようになった**時は何もしない（固定しない。次のサイクルで評価）。

### 6.4 固定中の再計算（洗い替え）
- マスタの M・T・係数の刻み・丸めを変えた → 固定した D で係数を再計算。
- 基本額（カテゴリ金額・手入力額）が変わった → 補正推奨額を再計算。
- 固定時に条件外だった銘柄は 1.00 のまま（条件判定も固定値を使う）。

### 6.5 確認ダイアログ文言（案）
- 外す: 「現在は 推奨額 80,000円（基本60,000円 × 係数1.35）で固定されています。固定を外すと、係数は時価総額に合わせて自動で更新されるようになります。現時点の値では 推奨額 75,000円（係数1.25）です。」
- 付ける: 「推奨額 75,000円（基本60,000円 × 係数1.25、1位まで18.2倍）で固定します。次に高値を更新するまで係数は変わりません。」

## 7. 履歴（新トップレベルキー `capCoefHistory`）

- レコード: `{ id, securityId, state:'live'|'fixed', D, eligible, coef, baseAmount, amount, ccy, fixedAt, trigger:'buy'|'migrate'|'manual'|null, txnId, reason, createdAt, updatedAt, deleted? }`
- **書き込むのは係数か補正推奨額が変わった時だけ**（同期負荷を抑える。表示は常に都度計算）。
  - 変動中: 最新の live レコードを上書き。
  - 固定中: 新レコードを追加（reason: 固定／マスタ変更／基本額変更／固定解除 等）。
- 固定状態の正は「最新レコードの state」。チェックボックスはこれを表示・操作する。
- 同期: `sync-merge.js` の `SCHEMA` に `capCoefHistory: ['records', (r) => \`cc:${r.id}\`]` を登録。id は端末間で衝突しない形（securityId＋時刻＋乱数）。削除はトンボストン（ルール7-2）。
- 書き込み契機: 価格・時価総額の更新後／取引の登録・取込・削除／銘柄編集／マスタ設定変更／起動時（移行・高値解除チェック）。
- 表示: 銘柄詳細ドロワーに「補正係数の履歴」表（分析履歴と同様）。

## 8. 実装ステップ

1. 設定 `settings.capCoef` の既定値・マスタ画面 UI（条件の格付/カテゴリ select・かつ/または・M・T・係数の刻み・丸め）。
2. `calc.capCoef(sec)` / `calc.capAmount(sec)`（状態・D・条件・丸めを返す純関数＋_calcMemo 対応）。
3. `capCoefHistory` 初期化・SCHEMA 登録・`syncCapCoef(secIds?)`（差分がある時だけ書く）。
4. フック: `store.addTransaction` / `removeTransaction`（取込経路含む）、価格・情報更新完了、銘柄保存、マスタ保存、起動時移行。
5. 条件外れ確認画面（取込・銘柄編集・マスタ条件変更の後に一括表示）。
6. 列 `capCoef`/`capAmount`（§5 の配線）。
7. 銘柄詳細ドロワー: 係数の内訳（基本×係数＝額・D・固定状態）＋固定チェック＋履歴表。銘柄編集フォームにも固定チェック。
8. `DESIGN.md`（判定ロジック・データモデル）・`REQUIREMENTS.md` 反映。
9. `APP_VERSION`・`?v=` 更新。

## 9. テスト観点

- 係数の境界（D=200/100/1/0.5・未取得）、刻み 0.05／0.1 での切り捨て（1.10 が 1.05 に落ちる等の浮動小数誤差がないこと）、丸め（JP 5,000／US $50／係数1.00は丸めない）。
- 条件（かつ/または、格付・カテゴリ境界、カテゴリ改名）。
- 固定: 初回買い→固定／買い増し→同係数／高値更新→解除／条件外で買い→1.00固定／買い削除→戻る／手動ON・OFFダイアログ／D未取得の保留。
- 洗い替え: M・T・丸め変更、カテゴリ金額変更、手入力額変更で履歴が1件ずつ増える。変動中は上書きのみ。
- 条件外れ確認画面（取込・編集・マスタ条件）。
- 移行（取引あり／なし／最後の買い後に高値更新済み）。
- 同期: 2端末で別銘柄を更新しても消えない（`main.content` 収まり・全タブ表示も確認）。
- 起動するか（window エクスポート漏れで白画面にならないか）。
