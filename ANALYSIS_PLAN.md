# 分析タブ（チャートパターン判定・テクニカル分析シグナル）実装計画

最終更新: 2026-06-23 / 作業ブランチ: `claude/securities-portfolio-tool-WGIsF`
Notionタスク: [分析タブ：チャートパターン判定（テクニカル分析シグナル）フェーズA](https://app.notion.com/p/3874a4a7eb9f81d79ae9fc2012893a57)（ID自動採番 `SEC-xxx`）

本書は「保有銘柄・銘柄マスタの中から、チャートパターン（テクニカル分析）に基づく買い候補シグナルを機械判定し、0〜100点でスコア化して表示する新『分析』タブ」の実装計画。
**次セッションのClaudeはこの1枚＋Notionタスクを読めば、何をどの順でやるか把握できる**ことを目的とする。
（全体アーキテクチャの原典は `REQUIREMENTS.md` / `DESIGN.md`。本書はテクニカル分析観点で具体化したもの）

---

## 0. ゴール（最重要要件）

- 長期投資の用途。テクニカルは「**買う銘柄を選ぶ道具**」ではなく「**買いたい銘柄をどこで買うか決める道具**」として使う。
- ファンダで選んだ銘柄群に対し、**チャートパターンの完成度をスコア化**して「今どの形に近いか／ブレイク済みか」を一覧する。
- **完全一致の二択ではなく、スコア（0〜100）＋ステータス（形成中/完成間近/ブレイク済み/失敗）**で候補を絞り込む。
- 「分析」ボタンで**まだ当日分析していない銘柄のみ**を計算（無駄な再計算をしない）。
- スコアを日付ごとに積み上げ、「**この銘柄のカップ・ウィズ・ハンドルのスコアが3週連続で上がっている**」というスコアの時系列を見られるようにする。

---

## 1. 方針（2026-06-23 すみぽんと合意）

| 論点 | 決定 |
| --- | --- |
| 段階 | **フェーズAから始める**（結果のみ保存）。生履歴の差分積み上げ＝フェーズBは、必要と判明してから |
| 計算場所 | **クライアント側**（現構成＝バニラJS＋localStorageと相性◎） |
| 保存先 | **Googleドライブ JSON**（OAuth＝Google許可承認が必要、フェーズ1のGoogle連携基盤と共通）。※後述§5に「当面は `_appdata` 相乗り」の代替案あり＝**要確認の論点** |
| 対象パターン | **優先上位4種からスタート**。全10種は同じデータ（日足OHLCV）から導けるので、基盤は1回作れば使い回せる |

### フェーズの考え方（再掲）
- **フェーズA（今回）**: 「分析」ボタンで2年分の日足を都度取得 → メモリで指標・パターン計算 → **結果（スコア・ステータス・根拠）だけ**を保存。複雑さ小・価値大。
  - 利点: 分割・配当による過去価格の再調整問題が**起きない**（結果は確定値だから）。スコアの時系列が自然に積み上がる。
- **フェーズB（必要になったら）**: 生の日足OHLCを差分積み上げ保存。分割・配当の再調整対応が必須になる（§7）。

---

## 2. 確定アーキテクチャ

```
[分析タブ] フィルタで対象銘柄を絞る
   ↓ 「分析」ボタン
[クライアント] 対象のうち「当日未分析」の銘柄だけループ
   ↓ /api/history で2年分の日足OHLCVを取得（銘柄ごと）
[クライアント] 共通部品で指標を計算（週足・MA・ATR・平均出来高・ピボット）
   ↓
[クライアント] 各パターンの条件を数値判定 → 条件ごと加点 → スコア0〜100 + ステータス
   ↓
[保存] 結果（スコア/ステータス/根拠/分析日）を保存（ドライブJSON or _appdata）
   ↓
[分析タブ] 表に表示・パターン別ソート・スコア時系列
```

- 計算は純JavaScript。**外部ライブラリ不要**（チャート描画も既存のバニラSVG）。
- ボトルネックは1つだけ: 現 `/api/history` が**終値しか返さない**。OHLCV返却への改修が前提条件。

---

## 3. データ基盤（共通部品・全パターンで使い回す）

### 3.1 `/api/history` を OHLCV 返却に改修【唯一のサーバ側変更】
現状（`functions/api/history.js`）は `points:[[unixSec, close], ...]` を返す。
- **後方互換**で `?format=ohlcv` パラメータを追加。指定時は始値・高値・安値・終値・出来高・調整後終値を返す。
  ```
  GET /api/history?symbol=AAPL&range=2y&interval=1d&format=ohlcv
  → { symbol, bars:[{ t, o, h, l, c, v, adj }, ...], meta:{ price, currency } }
  ```
- Yahoo chart の `indicators.quote[0]` に `open/high/low/close/volume`、`indicators.adjclose[0].adjclose` が既にある（取得済みだが未公開なだけ）。
- 既存の終値専用 `points` 形式（`detailSvgChart` が使用）はそのまま温存。`format` 未指定時は従来通り。
- キャッシュTTLは現状600s据え置き。

### 3.2 取得データ
- **日足を取得**（range=2y, interval=1d）。長期パターンは最大~325営業日・52週を見るので2年で足りる。足りない場合のみ range を伸ばす。
- **週足は日足から計算で生成**（保存・二重管理しない）。週は「金曜終値・週内高安・週内出来高合計」で集約。
- 計算で導く指標（読み込み時に算出）:
  - 移動平均: 25日 / 75日 / 200日、13週 / 26週 / 52週
  - ATR（真の値幅の平均）
  - 平均出来高: 20日 / 50日 / 150日
  - 調整後終値（分割・配当の連続性確保のため、パターン判定は原則 adj を使う）

### 3.3 ピボット（山・谷）検出
チャートパターン判定の基礎。**終値ベース**で安定させる（ヒゲによる誤判定を避ける）。
- ピボット高値: ある足の終値が前後N足の中で最大
- ピボット安値: ある足の終値が前後N足の中で最小
- N: 日足は前後5、週足は前後2〜3

### 3.4 許容誤差パラメータ（定数として一元管理）
チャートパターンは完全一致しないため許容幅が必須。小型・グロースは広め、大型は狭め。

| 判定対象 | 許容誤差 |
| --- | --- |
| ダブルボトムの2安値 / ダブルトップの2高値 | 3〜8% |
| レンジ上限・下限 | 3〜7% |
| ネックライン突破 | 2〜5%以上 |
| ブレイク判定（終値） | 1〜3%以上の上抜け |
| 出来高増加 | 20日平均の1.5倍以上 |

---

## 4. パターン別 機械判定条件（数値定義）

各パターン: **条件ごとに加点し0〜100点**。ステータスは `0=該当なし / 1=形成中 / 2=完成間近 / 3=ブレイク済み / 4=失敗`。
判定基準: **80点以上=有力 / 60〜79=候補 / 40〜59=弱い候補 / 40未満=除外**。

### フェーズA-1（優先上位4種・まず実装）

#### ① カップウィズハンドル（最重視・成長株/テーマ株向き）
構造: `左高値 → カップ底 → 右高値 → ハンドル安値 → ブレイク`
- 事前上昇あり: 過去3〜12か月で30%以上上昇、または左高値時点で株価>200日線かつ200日線が上向き … **+15**
- カップ深さ適正: `cup_depth=(左高値-カップ底)/左高値` が 12〜35%（許容10〜50%） … **+15**
- カップ期間適正: 日足35〜325営業日 / 週足7〜65週 … **+10**
- U字型（V字でない）: カップ底+10%以内の価格帯に全期間の20%以上の日数が存在、かつ `0.4 ≤ 左側期間/右側期間 ≤ 2.5`。底→右高値の回復が全期間の20%未満で完了ならV字として除外 … **+15**
- 右高値が左高値に近い: `右高値 ≥ 左高値×0.90`（最低0.85） … **+10**
- ハンドルが浅い: `handle_depth=(右高値-ハンドル安値)/右高値` が 3〜15%（許容〜20%） … **+10**
- ハンドル中の出来高減: ハンドル平均出来高 < カップ右側平均出来高（or 50日平均） … **+10**
- ブレイク時の出来高増: `終値>ハンドル高値×1.02` かつ `出来高>20日平均×1.5`（週足終値突破ならより強い） … **+15**

#### ② レンジブレイク（長期もみ合い銘柄向き）
構造: 一定期間レンジ内推移 → 上限を出来高付きで突破
- レンジ期間十分: 日足60〜250営業日 / 週足12〜52週 … **+15**
- レンジ幅適正: `(上限-下限)/下限` が 10〜50%（長期は15〜40%が使いやすい） … **+10**
- 上限接触2回以上（理想3回）: 上限±5%に複数回接近 … **+15**
- 下限反発2回以上: 下限±5%で複数回反発 … **+10**
- レンジ内滞在率: 対象期間の終値の80%以上が下限〜上限内 … **+15**
- ブレイク終値確認: `終値>上限×1.02`（大型1〜2%/小型3〜5%） … **+20**
- ブレイク時出来高増: `出来高>20日平均×1.5` … **+15**
- （ブレイク後5〜10日 `終値>上限×0.98` 維持で確度UP＝失敗判定の材料）

#### ③ ダブルボトム（大幅下落後の底打ち向き）
構造: `第1安値 → 反発高値(ネックライン) → 第2安値 → ネックライン突破`
- 事前下落あり: 過去3〜12か月で20%以上下落、または株価<200日線かつ200日線が下/横 … **+10**
- 2安値が近い: `abs(low1-low2)/low1 ≤ 0.08`（理想5%以内） … **+20**
- 第2安値が第1安値を大きく割らない: `low2 ≥ low1×0.97`（`low2<low1×0.92`なら失敗＝下落継続） … **+15**
- 谷の間隔適正: 日足20〜120営業日 / 週足4〜24週 … **+10**
- 中間反発十分: `(neckline-low1)/low1 ≥ 0.10`（大型5〜8%可） … **+10**
- 第2安値の出来高が減少: 第2安値周辺 ≤ 第1安値周辺（売り枯れ。重要） … **+15**
- ネックライン突破: `終値>neckline×1.02` … **+15**
- 突破時出来高増: `出来高>20日平均×1.3`（理想1.5倍） … **+5**

#### ④ アセンディングトライアングル（強い上昇トレンド銘柄向き）
構造: 水平な上値抵抗線 + 切り上がる安値 + 抵抗線突破
- 上値抵抗線が水平: 複数ピボット高値のばらつき `max-min ≤ 平均価格×0.05` … **+20**
- 安値が切り上がり: ピボット安値2つ以上（理想3つ）で `low1<low2<low3`、または安値回帰直線の傾き>0 … **+25**
- 形成期間適正: 日足20〜120営業日 / 週足4〜24週 … **+10**
- 値幅縮小: `直近の高安幅 < 初期の高安幅×0.7` … **+10**
- ブレイク確認: `終値>抵抗線×1.02` … **+20**
- 出来高増: `出来高>20日平均×1.5` … **+15**

### フェーズA-2（残りの買い候補パターン・順次追加）

#### ⑤ 逆三尊（下落トレンドからの反転）
構造: `左肩 → ネックライン → 頭(最深) → ネックライン → 右肩 → ネックライン突破`
- 中央の安値が最深: `head_low<左肩 かつ head_low<右肩`
- 左右の肩が近い: `abs(左肩-右肩)/head_low ≤ 0.15`（理想10%）
- 右肩が頭を割らない: `右肩 > head_low×1.05`
- ネックライン明確・傾き緩やか: `abs(neckline2-neckline1)/neckline1 ≤ 0.15`
- 出来高: 左肩下落>頭>右肩（売り圧力減衰）、ブレイク時 `>20日平均×1.5`
- ネックライン突破: `終値>neckline×1.02`（週足終値突破で強い）
- ※実務上は「安値切り上げ＋戻り高値突破」として見る方が安定（解釈が曖昧になりやすい）

#### ⑥ ラウンドボトム（長期低迷からの回復）
- 長期下落後: 過去6〜24か月で30%以上下落
- 底値圏で長期滞在: 底値+15%以内に対象期間（3〜18か月）の30%以上の日数
- 安値更新が止まる: 直近3か月の最安値 > 過去6か月の最安値、またはピボット安値切り上げ
- 移動平均改善: `25日線>75日線` または週足 `13週線>26週線`、強条件 `株価>200日線`
- 出来高漸増: 直近50日平均 > 過去150日平均、または上昇日出来高>下落日出来高

#### ⑦ ベース・オン・ベース（強い成長株の浅い再ベース）
- 第1ベースのブレイク: 第1レンジ上限を終値で2%以上突破、`出来高>20日平均×1.5`
- 第2ベースが浅い: 調整率 ≤ 15%（許容20%）
- 第2ベース中に主要MA維持: `終値>50日線` または週足 `終値>10週/13週線`
- 第2ベース期間: 日足10〜50営業日 / 週足2〜10週
- 再ブレイク: `終値>第2ベース高値×1.02`、`出来高>20日平均×1.3`

#### ⑧ フラッグ・ペナント（急騰後の短期調整・追加買い向き）
- 急騰あり: 10〜30営業日で20%上昇（or 5〜15営業日で15%）、急騰期間平均出来高 `>50日平均×1.5`
- 調整が浅い: `flag_depth=(高値-安値)/高値` が 5〜20%（30%超は弱い）
- 調整期間が短い: 5〜25営業日 / 週足1〜5週
- 調整中に出来高減: フラッグ平均 < ポール平均
- 再ブレイク: `終値>フラッグ高値×1.02`、`出来高>20日平均×1.3`

### フェーズA-3（警戒・除外パターン＝売り/避ける判定）

#### ⑨ 三尊天井（ヘッドアンドショルダー）
- 中央高値が最高: `head_high>左肩 かつ >右肩`
- 右肩が頭を超えられない: `右肩 < head_high×0.95`
- 左右の肩が近い: `abs(左肩-右肩)/左肩 ≤ 0.15`
- ネックライン割れ: `終値<neckline×0.98`、`出来高>20日平均×1.3`で弱気度UP

#### ⑩ ダブルトップ（M字天井）
- 2高値が近い: `abs(high1-high2)/high1 ≤ 0.08`
- 第2高値で上抜け失敗: `high2 ≤ high1×1.03`
- 中間安値が明確: `(high1-neckline)/high1 ≥ 0.08`
- ネックライン割れ: `終値<neckline×0.98`

#### その他の警戒シグナル（数値判定が容易・スコアと別に旗で表示）
- 200日線割れ / 下降三角持ち合い（下値支持線が水平・高値切り下げ）/ 高値圏の大陰線・長い上ヒゲ（大出来高で翌日以降も戻せない）/ 出来高を伴う支持線割れ

### 長期投資向け 優先表示順
**買い候補**: ①カップ → ②レンジブレイク → ④アセンディング → ③ダブルボトム → ⑥ラウンドボトム → ⑤逆三尊 → ⑦ベースオンベース → ⑧フラッグ
**警戒・除外**: ⑨三尊天井 → ⑩ダブルトップ → 下降三角 → 高値圏大陰線/上ヒゲ → 200日線割れ → 出来高伴う支持線割れ

---

## 5. 保存設計（要確認の論点あり）

### 【重要・前提の訂正】既存の同期はすでにGoogleドライブ
当初「保存はSheets `_appdata` のみ・Drive未実装」と整理したが**誤り**。実コードは**すでにGoogleドライブ同期が稼働**している（[app.js:833 `syncNow`](app.js)、[sync-merge.js](sync-merge.js)）:
- 全状態バンドル `dataBundle()`（store.data＋`_colPrefs`）を Drive の **`data.json`** に保存
- 読込→**3-wayマージ**（`SyncMerge.mergeBundle(base, local, remote)`）→ローカル反映→書込→base更新、を25秒間隔＋タブ離脱時に自動実行
- 上書き前に**日次バックアップを最大5世代**（`backup-YYYYMMDD-HHMMSS.json`）
- 資産推移は別ファイル **`portfolio-history.json`**（＝別ファイル運用の前例が既にある）
- マージ規則は `sync-merge.js` の `SCHEMA`（records / map / single など）。**未登録キーは `single` 扱い**＝両端末で変えると local 丸ごと優先

→ したがって「案A＝相乗り」は **Sheets相乗りではなく、既存Drive同期(`data.json`)への相乗り**を指す。

### 保存する内容（フェーズA＝結果のみ）
銘柄ごとに、確定した結果だけを保存（生履歴は保存しない）。**サイズ有界化のため、当面は最新スナップショットのみ保持**（スコア時系列が必要になったら間引き or 別ファイル化）。
```
techAnalysis[priceKey] = {     // ※既存 analyses(ファンダ) と別名にして衝突回避
  lastAnalyzed: "2026-06-23",  // 当日未分析判定（最終確定取引日基準）
  best: { pattern:"cup", status:2, score:74 },  // 最有力パターン
  patterns: { cup:{score:74,status:2}, range:{...}, doubleBottom:{...}, ascTriangle:{...}, ... },
  evidence: { ma200Pos:"above", ma200Slope:"up", pivots:[...], dropPct, risePct,
              formDays, breakLevel, buyCandidate, failLevel, volCond },
  _updatedAt: "2026-06-23T08:00:00Z"  // map マージの新旧判定用に持たせてもよい
}
// スコア時系列が必要になった場合のみ、最新の外に history:[...] を上限N件で持つ（サイズ注意）
```

### 保存先の選択（更新版）
| 案 | 内容 | 追加インフラ | デメリット | 推奨 |
| --- | --- | --- | --- | --- |
| **A. 既存 `data.json` に相乗り** | 結果を `store.data.techAnalysis` に持たせ、既存Drive同期（`dataBundle`/`restoreBundle`/3-wayマージ/日次バックアップ）にただ乗り | **不要**（OAuth/Drive新設なし） | ①`data.json` 肥大（丸ごと書換＋バックアップ5世代に倍化）②localStorage圧迫 ③`sync-merge.js` SCHEMA未登録だと別端末の結果を上書きで失う | **推奨。①②は「最新のみ有界保持」で解消、③はSCHEMAに `map` 1行登録で解消** |
| **B. ドライブ別JSONファイル** | `analysis.json` 等で別管理（`portfolio-history.json` と同流儀。Drive I/Oは既存流用可） | 小（Drive I/O自体は既存。専用マージは自前） | 3-wayマージ・バックアップの自動適用外＝自前で実装する手間。フェーズAでは過剰 | データが大きくなる**フェーズB（生履歴）で採用** |

→ **提案: フェーズAは案A（`data.json` 相乗り＋最新スナップショットのみ有界保持）。** 生履歴を持つフェーズBで案B（別ファイル）に切り替える。

### 案Aで既存機能を壊さないための必須3点（CLAUDE.mdルール5）
1. **後方互換**: `store.load()` に `this.data.techAnalysis ||= {}` を追加（旧データに無いため）
2. **マージ配線**: `sync-merge.js` の `SCHEMA` に `techAnalysis: ['map', byUpdatedAt]` を1行追加（銘柄キー単位で3-wayマージ。未登録だと single 扱いで別端末の結果を失う）
3. **名前衝突回避**: 既存の `analyses`（ファンダ分析レコード）・銘柄項目 `analysisDate/analysisNote` と混同しないよう、テクニカル結果は **`techAnalysis`** とする
（用語: 3-wayマージ＝前回同期時点(base)・自端末(local)・ドライブ(remote)の3つを突き合わせ、衝突を銘柄単位で解決する仕組み。`data.json`＝全データを保存したドライブ上のファイル）

---

## 6. UI仕様（新「分析」タブ）

### タブ追加（app.js）
- `NAV_GROUPS[0].items`（1614〜1631行）に `{ id:'analysis', label:'分析', icon:'report' }` を追加
- `PAGE_TITLE`（1633〜1637行）に `analysis:'分析'` を追加
- `render()` のswitch（2204〜2224行）に `case 'analysis': renderAnalysis(); break;` を追加
- 表示は `renderMarket`（2632行〜）をベースに `renderAnalysis()` を新設

### フィルタ（保有銘柄タブのUIを流用）
- 市場（US/JP/全）、保有有無、証券会社、口座、カテゴリ、検索ボックス（既存 `holdingsSearch` 系を流用）
- フィルタで**分析対象を絞り込む** → 重い計算を必要な銘柄に限定

### 「分析」ボタン
- 押下で、フィルタ後の銘柄のうち **`analysis[priceKey].lastAnalyzed` が最終確定取引日より前のものだけ** を計算
- **場中の未確定バーは結果に含めない**（最終確定取引日基準で判定）
- 進捗表示（n/m 銘柄）。1銘柄ずつ `/api/history` を順次（or 軽い並列）取得 → 計算 → 結果保存
- 完了後に表を再描画

### 表のカラム
- 銘柄 / 市場 / 最有力パターン / ステータス（形成中/完成間近/ブレイク済み/失敗）/ スコア / 買い候補価格 / 失敗判定価格 / 分析日
- パターン別・スコア順でソート可
- 行クリックで詳細（根拠の内訳、スコア時系列のスパークライン）

### 出力イメージ（銘柄ごと詳細）
```
主要パターン: カップウィズハンドル
ステータス: 完成間近
信頼度: 74点
根拠:
 - 200日線との位置: 上、200日線は上向き
 - 左高値→カップ底の下落率28%、カップ形成18週、右高値は左高値の93%
 - ハンドル調整率8%、ハンドル中の出来高は50日平均を下回る
 - まだハンドル高値を終値で突破していない
買い候補価格: ハンドル高値を終値で2%以上突破＋出来高20日平均1.5倍以上
失敗判定: ハンドル安値を終値で3%以上割り込み
```

### チャートへの指標オーバーレイ（フェーズA-2以降）
- 既存 `detailSvgChart`（5436行〜）/ `loadDetailChart`（5410行〜）を拡張し、MA（200日/52週等）・検出したピボット・ネックライン・ブレイク水準を重ね描き
- 余力でRSI・MACDをサブチャート表示

---

## 7. 技術的な前提・落とし穴

- **唯一のサーバ側変更**: `/api/history` のOHLCV公開（§3.1）。これが全ての前提条件。
- **分割・配当の再調整問題（フェーズB専用の課題）**: 確定済みOHLCも、株式分割が起きると**過去すべての調整後株価が書き換わる**（配当でも調整後終値はズレる）。
  - フェーズA（結果のみ保存）は**この問題が起きない**（保存するのは確定したスコア）。
  - フェーズB（生履歴の差分積み上げ）に進む場合のみ対処が必要: **生（未調整）のOHLC＋分割イベントを保存し、読み込み時に調整**（or 分割検知時にその銘柄だけ全履歴を取り直す）。本PJには既に `applySplit` と `/api/splits` があるので連携する。
- **パフォーマンス**: 全銘柄×日足取得×10パターンは銘柄増で重くなる。対策＝「分析ボタン押下時に計算→結果キャッシュ」「フィルタで対象を絞る」「当日未分析のみ計算」。
- **誤検知の許容**: 長期投資は週1回スキャンで候補を10→3に絞る用途。誤検知20〜30%でも有用。見逃しより誤検知が許容しやすいので、閾値はやや緩めから始めて調整する。
- **カップウィズハンドルのU字判定**が最難関。V字除外条件（§4①）を厳しめにして誤検知を抑える。
- 対象市場は **JP/US のみ**（FUNDは除外済み）。Yahoo chart は両市場で日足OHLCV取得可。

---

## 8. 実装手順（フェーズ別・実施順）

> 依存順に上から。各ステップ完了で `npx wrangler pages dev .` で動作確認 → 次へ。

### フェーズA-0: データ基盤
1. `functions/api/history.js` に `?format=ohlcv` を追加（§3.1）。`/api/history?...&format=ohlcv` を実際に叩いてOHLCV配列が返ることを確認。
2. 指標計算の共通モジュール（純JS）を新設: 週足集約 / SMA / ATR / 平均出来高 / ピボット検出 / 許容誤差定数（§3.2〜3.4）。`functions/lib/` ではなくクライアント側（`app.js` 内 or 新 `indicators.js`）。

### フェーズA-1: 優先4パターン＋タブ
3. パターン判定（①②③④）をスコア関数として実装（§4）。1パターン1関数、共通部品を使い回す。
4. 新「分析」タブのUI（§6）: ナビ追加 → `renderAnalysis()` → フィルタ → 「分析」ボタン → 表。
5. 結果保存（§5案A: `store.data.analysis`）。当日未分析判定・スコア時系列の積み上げ。
6. 動作確認: 数銘柄で分析実行 → スコア・ステータス・根拠が出る。`index.html` の `?v=` を上げてキャッシュ更新。

### フェーズA-2: 残りパターン＋チャート指標
7. ⑤⑥⑦⑧ を追加（§4）。
8. `detailSvgChart`/`loadDetailChart` にMA・ピボット・ネックライン・ブレイク水準のオーバーレイ（§6）。余力でRSI/MACDサブチャート。

### フェーズA-3: 警戒パターン
9. ⑨⑩＋その他警戒シグナル（§4）。買い候補とは別に「警戒」フラグ列で表示。

### フェーズB（必要と判明したら・別計画化）
10. 生の日足OHLCを差分積み上げ保存。分割・配当の再調整対応（§7）。保存先を案B（ドライブJSON）へ。

---

## 9. 配線チェックリスト（CLAUDE.md ルール5準拠）

新タブ・新データ（`store.data.techAnalysis`）追加に伴い、以下を確認:
- [ ] **ナビ/ルーティング**: `NAV_GROUPS` / `PAGE_TITLE` / `render()` switch / `go()` の遷移
- [ ] **表の列**: 分析タブ専用の列定義・描画・ソート（既存 `MASTER_COLS`/`COL_RENDERERS` 流用 or 専用化を判断）
- [ ] **フィルタ**: 市場/保有/証券会社/口座/カテゴリ/検索（保有銘柄タブの状態 `st.*`・`holdingsSearch` を流用）
- [ ] **Google同期（重要）**: `techAnalysis` は `store.data` 配下なので `dataBundle()`/`restoreBundle()` で既存Drive同期(`data.json`)に乗る。ただし **`sync-merge.js` の `SCHEMA` に `techAnalysis: ['map', byUpdatedAt]` を登録必須**（未登録だと single 扱いで別端末の結果を上書きで失う）
- [ ] **保存/読込**: `store.save()`（localStorage `sm_data_v1`）、`store.load()` のデフォルト初期化に `techAnalysis ||= {}` を追加
- [ ] **名前衝突回避**: 既存 `analyses`（ファンダ分析）・銘柄項目 `analysisDate/analysisNote` と別物。キー名は `techAnalysis`
- [ ] **サイズ**: 結果は最新スナップショットのみ有界保持（`data.json` 肥大＋バックアップ5世代倍化を防ぐ）
- [ ] **キャッシュ更新**: `styles.css`/`app.js` 変更時は `index.html` の `?v=` を上げる（[[frontend-cache-bust]]）
- [ ] **API**: `/api/history?format=ohlcv` をローカル wrangler で実叩き確認（静的サーバーでは `/api/*` が動かない）
- [ ] 入力で手入力する項目は無い（分析結果は派生値・自動生成）ため、取込/一括変更/転記への配線は**不要**（理由: 手入力ルートが無く、再計算で再現可能なため）

---

## 10. 着手前の確認事項（要すみぽん判断）

1. **保存先**: フェーズAは案A（既存Drive `data.json` 相乗り＋最新スナップショットのみ有界保持＝新インフラ無し）でよいか。別ファイル化（案B）は生履歴のフェーズBから、で合意できるか（§5）。
2. **対象パターンの初期スコープ**: フェーズA-1の4種（①②③④）で着手し、動作確認後にA-2/A-3を足す進め方でよいか。
3. **取得レンジ**: 2年（range=2y, 日足）で開始してよいか（長期パターンで不足が出たら伸ばす）。
4. **しきい値の初期値**: §4の数値（ChatGPT整理）をそのまま初期値として実装し、実データを見て調整する方針でよいか。

---

## 11. 参照
- `REQUIREMENTS.md`（要件）/ `DESIGN.md`（詳細設計）/ `HANDOFF.md`（引き継ぎ）
- `NOTIFICATION_PLAN.md`（通知計画。Google連携・サービスアカウントの土台が共通）
- 主要コード位置（app.js）: ナビ 1614-1637 / `render()` 2204-2224 / `renderMarket` 2632 / `detailSvgChart` 5436-5473 / `loadDetailChart` 5410-5426 / `dataBundle`/`restoreBundle` 1705-1713 / `priceKey` 1254 / `STORAGE_KEY` 16 / `store.save` 257
- `functions/api/history.js`（OHLCV改修対象）/ `functions/api/price.js`（OHLC取得ロジックの参考）/ `functions/api/splits.js`（フェーズBの分割連携）
- Notionタスク本文（時系列の議論ログ）
