# 証券管理ツール 要件定義書

最終更新: 2026-05-29
ステータス: ドラフト（要件整理フェーズ／実装着手前）

---

## 1. 目的・背景

自身が保有する株式（日本株・米国株）について、株価変動と資産状況を
ほぼリアルタイムに把握し、あらかじめ定めた**買い増しルール**に基づいて
購入タイミングを逃さないよう通知を受け取ることを目的とする。

最重要要件は **銘柄ごとの買い増しルール管理と、買い増し価格到達時の通知**。

---

## 2. 確定した前提（ヒアリング結果）

| 項目 | 決定事項 | 補足 |
|------|----------|------|
| 株価の鮮度 | **15〜20分遅延でOK** | 無料データソースで実現可能。リアルタイム証券会社APIは使わない |
| 通知手段 | **LINE通知 ＋ メール通知** | 両方に配信 |
| ホスティング | **Cloudflare Pages** を利用 | スマホ/PC両対応はブラウザアクセスで実現 |
| データ取込 | **SBI証券・楽天証券・moomoo証券・汎用CSV ＋ 手入力** | CSVは列マッピング対応で吸収 |

---

## 3. 機能要件

### F1. ポートフォリオ管理
- 保有銘柄の登録（日本株 / 米国株）
- 保有数量、取得単価（平均取得単価）、取得日を管理
- 現在値・前日比・評価額・評価損益（金額/％）を表示
- 日本株は円、米国株はドル建て。**USD/JPY 為替**で円換算し、合計資産を円で表示
- 銘柄別 / 全体のサマリー表示

### F2. 株価の準リアルタイム取得（15〜20分遅延）
- データソース: Yahoo Finance 系の公開エンドポイント（無料・遅延あり）を想定
  - 日本株: ティッカー + `.T`（例: トヨタ = `7203.T`）
  - 米国株: ティッカー（例: `AAPL`）
  - 為替: `USDJPY=X`
- Cloudflare の **Cron Triggers** で定期ポーリング（例: 取引時間中は数分〜15分間隔）
- 取得値は D1 に保存し、画面表示と買い増し判定の両方で利用

### F3. マルチデバイス参照
- Cloudflare Pages にホストしたレスポンシブWebアプリ
- スマホ / PC のブラウザから同一データを参照（バックエンド + DBで一元管理）
- 個人利用前提。簡易な認証（パスワード or トークン）で保護

### F4. データ取込
- **手入力**: 銘柄・数量・取得単価を画面から登録/編集
- **CSV取込**: SBI / 楽天 / moomoo の保有証券CSV、および汎用CSVに対応
  - 文字コード（Shift_JIS / UTF-8）と列マッピングを吸収するインポータを用意
  - 取込時に「新規追加 / 既存更新（数量・取得単価の上書き or 平均）」を選択

### F5.【最重要】買い増しルール管理と通知
詳細は「4. 買い増しルール仕様」を参照。

---

## 4. 買い増しルール仕様（確定提案 ※要確認）

### 4.1 基本ルール
- **初回購入トリガー**: 基準高値から **−40%** 下落で「初回購入サイン」
- **買い増しトリガー**: 前回購入価格から **−20%** 下落で「買い増しサイン」
- 下落率（40% / 20%）と買い増し金額は **銘柄ごとに設定変更可能**（左記はデフォルト値）

### 4.2 用語の定義（曖昧点の確定提案）
- **基準高値**: デフォルトは「取得可能な株価データ範囲での最高値」。
  - 銘柄ごとに「52週高値 / 上場来高値 / 手動指定」へ切替可能とする。
- **前回購入価格**: その銘柄の直近購入記録の約定単価。
  - 未購入（保有なし）の銘柄は 4.1 の「初回購入トリガー」を評価。
  - 1回以上購入済みの銘柄は「買い増しトリガー」を評価。

### 4.3 トリガー価格の算出
- 初回購入トリガー価格 = 基準高値 × (1 − 0.40)
- 買い増しトリガー価格 = 前回購入価格 × (1 − 0.20)
- 現在値が当該トリガー価格 **以下** になったらサイン発火

### 4.4 サインの状態管理（重複通知の防止）
- 一度発火したサインは「ステータス: 通知済み」とし、同一トリガーでは再通知しない
- 次の状態へ進むのは以下のいずれか:
  - ユーザーが「購入を記録」→ 前回購入価格が更新され、次のトリガーを再計算
  - ユーザーが「スヌーズ / 却下」→ 一定期間（例: 当日）再通知しない
- 株価がトリガー価格を上回って戻った後、再度割り込んだ場合は再通知（設定で切替可）

### 4.5 買い増し金額の管理
- 各回の買い増し予定金額（定額 or ルール）を銘柄ごとに設定
- 実際の購入記録（約定単価・数量・金額・日付）を履歴として保存
- 累計投資額・平均取得単価を自動計算

### 4.6 通知内容（LINE / メール 共通）
- 銘柄名・ティッカー / サイン種別（初回 or 買い増し） / 現在値 / トリガー価格 /
  基準値（基準高値 or 前回購入価格） / 推奨買い増し金額 / 判定時刻

---

## 5. 非機能要件
- **対応端末**: モダンブラウザ（スマホ/PC）。レスポンシブ対応
- **認証**: 個人利用前提の簡易認証（不正アクセス防止）
- **コスト**: 無料〜低コスト（Cloudflare 無料枠 + 無料株価データソースを基本）
- **データ保全**: D1 のバックアップ手段を用意（エクスポート機能 等）
- **タイムゾーン**: JST 基準で表示。米国市場の取引時間も考慮

---

## 6. 技術構成（案）

| レイヤ | 採用候補 | 備考 |
|--------|----------|------|
| ホスティング | Cloudflare Pages | フロント配信 |
| API | Pages Functions / Workers | REST API |
| DB | Cloudflare D1（SQLite） | 保有・購入履歴・ルール・価格キャッシュ |
| 定期実行 | Cloudflare Cron Triggers（Worker） | 株価ポーリング・買い増し判定・通知発火 |
| フロント | レスポンシブSPA（React/Vite 等） | スマホ/PC両対応 |
| 株価データ | Yahoo Finance 系公開API | 日本株`.T`/米国株/為替、15〜20分遅延 |
| LINE通知 | LINE Messaging API（push） | チャネルアクセストークン必要 |
| メール通知 | Resend など（Workersから送信可能なサービス） | Cloudflareは直接SMTP送信不可のため外部サービス利用 |

> 注: Cloudflare はメールの直接送信ができないため、メール通知は Resend 等の
> 外部APIを利用する。LINE/メールとも送信元の登録・APIキー設定が別途必要。

---

## 7. データモデル（初期案）

- **holdings**（保有）: id, market(JP/US), ticker, name, quantity, avg_cost, currency, created_at, updated_at
- **transactions**（購入/売却履歴）: id, holding_id, type(buy/sell), price, quantity, amount, traded_at, source(manual/csv)
- **rules**（買い増しルール）: id, holding_id(or ticker), initial_drop_pct(=40), addon_drop_pct(=20), base_high_mode(data/52w/listing/manual), base_high_value(manual時), addon_amount, enabled
- **prices**（価格キャッシュ）: ticker, price, prev_close, high_ref, fetched_at
- **signals**（サイン状態）: id, holding_id, type(initial/addon), trigger_price, status(pending/notified/snoozed/done), fired_at, notified_at
- **fx**（為替）: pair(USDJPY), rate, fetched_at
- **settings**（通知先など）: line_token, email_to, auth など（機密はSecretsで管理）

---

## 8. 未確定・要確認事項（次のステップ）

1. **基準高値の定義**: デフォルト「データ範囲の最高値」で良いか、52週高値が良いか
2. **買い増し金額**: 全銘柄共通の定額か、銘柄ごとに変えるか
3. **売却・一部売却**の扱い（前回購入価格・平均取得単価への影響）をどこまで管理するか
4. **認証方式**: 単一パスワードで十分か（個人利用想定）
5. CSVの実物フォーマット（SBI/楽天/moomoo）のサンプル提供可否
6. LINE / メール送信用のアカウント（LINE公式アカウント、Resend等）の準備可否

---

## 9. 想定スコープ（フェーズ分け案）

- **フェーズ1（MVP）**: 手入力での保有登録 → 株価自動取得 → 評価額表示 →
  買い増しルール判定 → LINE/メール通知（核心機能）
- **フェーズ2**: CSV取込（SBI/楽天/moomoo/汎用）、為替円換算の精緻化、履歴管理
- **フェーズ3**: チャート表示、サマリー強化、バックアップ/エクスポート、認証強化
