# 通知機能 実装計画（買い増しサインの自動通知）

最終更新: 2026-06-03 / 作業ブランチ: `claude/securities-portfolio-tool-WGIsF`
本書は「ツール未起動でも、買い増しサイン到達時に通知が届く」機能の実装計画。
**次セッションのClaudeはこの1枚＋Notionタスクを読めば、何をどの順でやるか把握できる**ことを目的とする。
（全体アーキテクチャの原典は `REQUIREMENTS.md` §5–7 / `DESIGN.md` §1–3。本書はそれを通知観点で具体化したもの）

---

## 0. ゴール（最重要要件）
- 銘柄ごとの買い増しルールに基づき、**買い増し価格に到達したら通知**する。
- **ツール（ブラウザ）を開いていない時間でも届く**こと（＝サーバ側の定時処理が必須）。
- 通知は**市場別の定時バッチ**：日本株 7:45 / 11:00 / 17:00、米国株 24:00 / 7:00（JST）。
- 重複通知の防止（rearm）：一度発火したサインは「通知済み」、同一トリガーでは再通知しない。価格が戻って再割込みしたら再通知（設定で切替）。

## 1. なぜサーバ側が必須か（制約）
- 現状はバニラJS＋localStorageの**ブラウザ完結**。閉じると何も動かず、データも端末内のみ。
- 通知のためには、サーバ側の定時処理が**自前で**：
  1. データ（保有・ルール・カテゴリ金額）を読む
  2. **最新株価・5年高値を自分で取得**（ツールのキャッシュに依存しない）
  3. 買い増し判定を計算
  4. 該当銘柄を集めて送信
- つまり「価格取得＋判定ロジック」を**サーバ側にも持たせる**（DESIGN: 共通モジュール化して両者から呼ぶ）。

## 2. 確定アーキテクチャ（再決定不要・docsと一致）
- **データの正＝Google スプレッドシート**（システム・オブ・レコード）。
  - 現状の保存方式（確定・実装済み）：ブラウザのGoogleログイン(GIS)で、全状態JSONを `_appdata` シートの A列に分割保存（`dataBundle()`＝store.data＋列設定colPrefs）。
- **サーバからのデータ読み取り＝サービスアカウント**（DESIGN §2.4）。
  - Cron/Functions が **サービスアカウント鍵**（CF Secrets保管）で同じスプレッドシートの `_appdata` を読み、`JSON.parse` して全データを得る。**人が読める表は不要**（通知目的では _appdata のJSONブロブをそのまま使う）。
  - サービスアカウントのメールに、対象スプレッドシートを**閲覧（できれば編集）共有**しておく。
- **価格キャッシュ＝Cloudflare KV**（頻繁更新の一時データ。Sheetsには書かない）。
- **定時実行＝Cloudflare Cron Trigger（Worker）**：価格取得→KV更新→判定→サイン記録(_appdata or KV)→定時通知。
- **通知チャネル＝LINE Messaging API ＋ メール(Resend)**（両方 or どちらか。**最終チャネルは未決定。下記§6**）。
- **コスト＝無料**（Cloudflare無料枠＝Workers/Cron/KV、Resend無料枠、LINE無料枠、株価は無料ソース）。
- 判定ロジックは現状 `app.js` の `calc.evaluate` / `signalRows` 等。サーバ用に**共通ロジックを切り出す**。

## 3. 現状（実装済み・2026-06-03時点）
- フロント機能はほぼ完成（UIデザイン刷新P1–P6、保有/サイン/レポート/取込/転記/銘柄マスタ/投信）。
- **本番デプロイ済み**：Cloudflare Pages（GitHub Actions自動デプロイ）。URLは推測回避のランダム名 `securities-manager-9fno6acdqirgdgb76qks.pages.dev`。push毎に自動更新。
- **Google連携(GIS)で _appdata にJSONブロブ保存/読込が動作**（列設定も含む。1セル5万字制限はA列分割で回避）。
- 価格・銘柄情報の取得は Cloudflare Pages Functions（`functions/api/price.js` `info.js` `history.js`）として存在＝**Cronから流用可能**。
- まだ無い：KV、Cron Worker、サービスアカウント読取、通知送信、サイン状態の永続化(rearm)。

## 4. 残作業（フェーズ別・実施順）
> 依存順に上から。各フェーズ完了で動作確認→次へ。

### フェーズN1: サーバからデータを読めるようにする（土台）
- すみぽん準備：Google Cloud で**サービスアカウント**作成→鍵(JSON)発行→対象スプレッドシートをそのSAメールに共有。
- Claude：CF Secrets に SA鍵を登録。Pages Function or Worker から `_appdata` を読み `JSON.parse` して store 相当データを得る検証。
- 完了条件：サーバ側で「自分の保有・ルール」をログ出力できる。

### フェーズN2: 価格取得＋判定ロジックのサーバ移植（共通化）
- Claude：`calc.evaluate`/`signalRows` 等の判定を、Functions/Worker から呼べる**共通モジュール**に切り出し（5年高値・トリガー・残り下落率・reached/near）。
- 価格・5年高値はサーバが自前取得（既存 price.js / history.js のロジック流用）。結果は**KV**にキャッシュ。
- 完了条件：サーバ単体で「到達/もうすぐ」の銘柄一覧を算出できる。

### フェーズN3: 通知送信
- すみぽん準備：通知チャネルの資格情報（§6で選定）。CF Secrets に登録。
- Claude：到達サインを整形して送信（LINE push / Resend メール）。文面は REQUIREMENTS §4.6 準拠。
- 完了条件：手動トリガーで実機に通知が届く。

### フェーズN4: 定時バッチ＆重複防止
- Claude：Cloudflare **Cron Trigger** で §0 の時刻に自動実行。サイン状態(pending/notified/expired, rearm)を `_appdata`(or KV) に記録し重複通知を防止。
- 完了条件：指定時刻に自動で、未通知サインのみが届く。

### フェーズN5（任意・将来）: 体験向上
- 起動中ポーリングでシート⇄ツール同期、人が読める表（保有/取引等）の出力、資産推移グラフ、Web Push/PWA。

## 5. すみぽんに用意してもらうもの（外部準備）
- (a) **通知チャネルの選定と資格情報**（§6）。
- (b) **Googleサービスアカウント**＋鍵(JSON)＋対象スプレッドシートの共有（フェーズN1）。
- (c) 既存：Cloudflare（デプロイ済み）、Google OAuthクライアントID（連携用）、対象スプレッドシート（作成済み: `11gm8X0LzZahEZIa131BUXnSzBjgGCdB4W64m__j2LMM`）。

## 6. 未決定事項（要すみぽん判断）
- **通知チャネル**：LINE / メール(Resend) / 両方。
  - LINE＝スマホ通知が即・確実だがMessaging APIのチャネル作成がやや手間（LINE Notifyは終了）。
  - メール(Resend)＝設定容易・無料枠あり。
  - **おすすめ初手**：まずメール(Resend)で動かし、後でLINE追加。
- 通知文面の詳細フォーマット（銘柄・現在値・トリガー・残り下落率・推奨額）。
- サイン状態の保存先（`_appdata` 内に signals セクション追加 / KV）。

## 7. 参照
- `REQUIREMENTS.md` §4.4(重複通知) §4.6/4.7(通知内容/タイミング) §5–7(アーキテクチャ/データ)
- `DESIGN.md` §1(構成) §2(Sheetsスキーマ/§2.4 サービスアカウント) §3(判定・通知ロジック)
- Notionタスク（「通知」「データ保存」「認証」「インフラ・デプロイ」カテゴリ）: 各フェーズの起票・ボール・状態
