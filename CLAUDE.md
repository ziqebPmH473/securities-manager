# 証券管理ツール - 開発ガイドライン

このファイルは、証券管理ツール（`C:\work\securities-manager`）を開発する Claude 向けの指示書です。

## プロジェクト概要

**プロジェクト名**: 証券管理ツール（Securities Manager）
**説明**: 保有株（日本株・米国株・投資信託）の資産把握と、銘柄ごとの買い増しルールに基づく「買い時（買い増しサイン）」管理ツール
**技術スタック**:
- フロントエンド: バニラ HTML / CSS / JavaScript（`index.html` / `app.js` / `styles.css`、ビルドステップなし）
- バックエンド: Cloudflare Pages Functions（`functions/api/price.js`＝価格、`functions/api/info.js`＝銘柄情報）
- データ保存: 現在 localStorage（将来 Google スプレッドシートへ移行予定）
- 株価データ: Yahoo Finance（日本株/投信/為替）、Finnhub（米株・APIキー設定時）

**リポジトリ**: GitHub `ziqebPmH473/securities-manager`（作業ブランチ `claude/securities-portfolio-tool-WGIsF`）
**主要ドキュメント**: `REQUIREMENTS.md`（要件）/ `DESIGN.md`（詳細設計）/ `HANDOFF.md`（引き継ぎ）

---

## タスク・バグ管理（Notion）

### タスクDB「証券管理ツールPJタスク」

**場所**: https://www.notion.so/d8bfa756b1f24fa5852e063e6d053a6e
（「📈 証券管理ツールPJ」ページ配下。data source: `collection://ebca306f-ea1a-4482-8256-7b09782dcfa8`）

**設計思想**:
- 「Claude ⇔ すみぽん」の2者間でタスク・バグ・議論を一元管理する
- **タスクとバグは同じDB**。`タスク種別`（🐛 バグ / ✨ 機能追加 / 🔧 改善 / 📝 ドキュメント / その他）で区別する
- 主ID = `ID` プロパティ（プレフィックス `SEC-` 自動採番）
- 未確定の相談は `議論中`、確定後 `起票`→`対応中`→`レビュー待ち`→`完了` に遷移

**DBプロパティ**:
- タイトル / ID（`SEC-xxx` 自動採番）/ ステータス（`議論中`/`起票`/`対応中`/`レビュー待ち`/`完了`/`不要`）/ タスク種別 / ボール（`Claude`/`すみぽん`）/ 優先度（`🔴S`/`🟠A`/`🟡B`/`🟢C`/`未設定`）/ カテゴリ（フロントUI / 価格・データ取得 / 判定ロジック / データ保存 / 認証 / 通知 / インフラ・デプロイ / ドキュメント）/ フェーズ（フェーズ1/2/3）/ 端末（`PC`/`スマホ` Multi）/ 作業場所（`📱 スマホ可`/`💻 PC必須`/`🔀 どちらも`）/ 所要時間 / 期限 / 再現条件（バグ用）/ 計画ファイル / 実装記録 / 完了日 / 起票日時（自動）/ 最終更新（自動）

### Claude の運用ルール

- **セッション開始時に「ボール=Claude かつ ステータス≠完了/不要」のタスクを確認**してから着手する
- タスク起票時は可能な限り `タスク種別` / `カテゴリ` / `優先度` / `フェーズ` / `端末` を埋める
- 新規実装タスクのワークフロー:
  1. Notion 起票（ステータス=`起票`、ボール=Claude）
  2. 実装開始時にステータス→`対応中`
  3. 実装完了＆動作確認済みで ステータス→`レビュー待ち`、ボール→すみぽん
  4. すみぽん確認後 `完了`（**完了への変更は原則すみぽん専権**。明示指示があれば Claude が完了化可）
- 1ページ=1タスク。議論・コメントはページ本文に時系列で追記（`YYYY-MM-DD HH:MM JST` プレフィックス）

### バグの遷移ルール（重要）

1. **着手時**: `起票` → `対応中` に変更
2. **実装完了＋push 時**: ステータスは `対応中` のままにする。**勝手に `完了` にしない**
3. **すみぽんが実機テストして OK を返した時のみ**: `完了` に変更 + `完了日` に当日日付（YYYY-MM-DD）を設定
4. **すみぽんが NG を返した時**: `対応中` のまま再修正。コミットは追補として続ける

**理由**: 実装完了 = 修正済ではない。実機での動作確認をすみぽんが行ってからが「完了」。
過去（動画ツールPJ）に修正済にした後すみぽん指摘で再openになるケースが多発したため。

**ボール運用**:
- 修正中のバグはボール=すみぽん（実機確認待ち）
- すみぽんから NG 指摘が来たらボール=Claude（再修正待ち）

**新規バグの起票**:
- 実装中に副次的に発見・即修正したバグ: 起票時から `完了`（完了日設定）で OK（起票と同タイミングでコミット済みのため）
- すみぽんが報告するバグ: 要点を整理して Claude が起票 → 通常フロー

---

## 開発ルール

### ルール1: 計画を立ててから実行する
- ユーザーのリクエストを受けたら、まず現在のコードを調査し、実装方針を決めてから着手する
- 大きな変更は対象ファイル・方針を先に提示して承認を得る
- **例外**: 1行修正・変数名変更など極めて明らかな修正は計画不要

### ルール2: 設計内容はドキュメントに記載する
- アーキテクチャの決定事項・データモデル・判定ロジックの変更は `DESIGN.md` に反映する
- 要件の変更は `REQUIREMENTS.md` に反映する

### ルール3: 実装後は自発的にテストする
- UI修正はローカルで実際に動かして目視確認する（`npx wrangler pages dev .` か `python -m http.server`）
- 価格・情報取得など API がからむ修正は `/api/price`・`/api/info` を実際に叩いて確認する
- 影響範囲（同種データの入力ルート・全市場タブ・サインタブ）を網羅的に確認する
- **例外**: ドキュメントのみ・1行 typo 修正はテスト省略可。迷ったら確認する

### ルール4: コミットは意味のある単位で、push 前に動作確認
- コミットメッセージ末尾に `Co-Authored-By: Claude ...` を付ける
- **実装・動作確認が済んだら、確認を取らずにコミット＆プッシュする**（このPJの方針：本番デプロイしないとすみぽんが実機確認できないため）。「プッシュしてよいですか？」と聞かない。
- 例外: 破壊的操作（force push・履歴改変・ブランチ削除等）や、まだ作業途中で壊れている状態は push しない

### ルール5: 項目（フィールド）を追加したら全配線先を必ず確認する
銘柄(security)・保有(holding)・銘柄情報(meta) に新しい項目を足す時は、**以下の配線先を1つずつチェックし、必要なものは漏れなく実装する**。「フォームに足して終わり」にしない（過去に prevBuyDate を列・取込に通し忘れた）。

**チェックリスト（app.js）**:
1. **銘柄編集フォーム** … 入力欄（`openSecurityForm`）＋保存patch（送信ハンドラの `patch = {...}`）
2. **表の列** … `MASTER_COLS`（列定義）＋ `COL_RENDERERS`（描画）＋ `DEFAULT_VISIBLE`（US/JP/SIGNAL の既定表示）＋ ソート `getSortVal` の `case`＋ `colDefaultWidth`（必要なら専用幅）
3. **取込** … 列選択式取込 `GI_FIELDS`／`GI_SEC_FIELDS`／`GI_GROUPS`／`GI_AUTOMAP`（＋必要なら `giParseValue` の型変換）／ 汎用往復 `GENERIC_MAP`・`GENERIC_HEADER`・`parseGeneric`・`exportGeneric`（HEADERと出力配列の**位置を一致**させる）／ 分析・保有取込 `ANALYSIS_COLMAP`・`HOLDING_COLMAP`
4. **一括変更** … `SM_BULK_FIELDS`／`holdBulk` 系＋ `bulkValueHtml`／`bulkConvert`
5. **銘柄詳細ドロワー** … 判定/保有/メタ/ファンダの表示（`renderDetail` 周辺）
6. **転記用/エクスポート** … 転記ビュー・貼付出力の列
7. **分割調整** … 金額系（価格・単価・前回購入価格・手動基準高値 等）は `applySplit` の調整対象に含めるか判断
8. **Google同期** … `dataBundle`/`restoreBundle`（store.data 全体を保存するので通常は自動。列設定 colPrefs に関わる項目だけ注意）

**判断基準**: 「自動取得・派生値・内部判定専用」なら列や取込は不要なこともある。だが**フォームで手入力できる項目は原則『表の列』『取込（GI＋汎用往復）』にも通す**（手入力↔取込↔表示の三者で一貫させる）。不要と判断した配線先は、その理由を実装記録かコメントに残す。

### ルール6: スクロールは「ページ全体ではなく枠内」。各タブは画面に収める
このアプリは**アプリシェル型**: `body{overflow:hidden}` でページ自体はスクロールせず、実際のスクロール領域は **`main.content{overflow-y:auto}`**（styles.css のビューポート固定セクション）。「ページ全体の縦スクロール」＝この `main.content` のスクロール。

- **基本方針**: どのタブも**外枠は常にブラウザ窓に収める**。`main.content` を極力スクロールさせない。
- **表形式（保有銘柄・マーケット・買い増しサイン・レポートの格付×カテゴリ）= 枠内スクロール**。表の外枠を窓の高さに収め、あふれる行は**表の枠の中だけ**でスクロールさせる（`thead` は sticky で残す）。
- **レポートの資産集計（グラフ＋表）= 1画面に収める**。グラフ高さを可変にして収め、内側スクロールは作らない。窓を小さくした時だけ `main.content` のスクロールを許容。
- **やってはいけない**: 行が少ない時に枠を `min-height` で画面下端まで引き伸ばして**無駄な余白**を作ること（過去に転記用で巨大余白＋ページスクロールが発生）。収まっている時は自然高さのまま。
- **収まり判定は必ず `main.content` 基準で測る**（`main.scrollHeight - main.clientHeight`）。`document`/`window` 基準は body が `overflow:hidden` のため**常に0になり誤判定**する（実際に何度も誤判定した）。プレビュー検証もこの値で確認する。
- 実装の要点: 表は `fitListTables()`（あふれた分だけ `.table-wrap` の max-height を詰める）、マトリックスは `fitMatrix()`（窓いっぱいに広げてはみ出し分だけ縮める自己補正）、グラフは `assetChartBox()`/`renderAssetChart()`（実はみ出し量でグラフ高さを補正）。

---

## コミュニケーションルール

### 返信は結論を先頭、詳細を後ろに
- すべての返信で、冒頭に結論または確認事項を出す（「〇〇してください」「〇〇でよいですか？」）
- 詳細・経緯・説明はその後ろに書く

### インフラ系の専門用語は噛み砕いて提示する
- Cloudflare・OAuth・サービスアカウント・KV 等の用語は、その場で1行で意味を説明する
- 選択肢を出す時はメリット・デメリットを生活レベルの比喩で示す
- 判断に必要な情報は省略せず、わかりやすく翻訳して提示する（「難しいから振らない」ではなく「噛み砕いて振る」）

### 質問のフレームを疑う
- 規約・法務・課金・個人情報がからむ話、既存仕様に紐づく話では、答える前に「前提や仕様そのものに問題はないか」を1回チェックする

---

## デバッグ方針

### 同じ症状が2回続いたら仮説を切り替える
- 「修正後も NG」の報告を2回以上受けたら、それまでの仮説路線に固執せず別の原因軸に切り替える
- ユーザー側（キャッシュ等）を疑う前に、自分のコード理解の盲点を疑う
- 「キャッシュをクリアしてください」を2回以上言わない。1回で解決しなければ別原因

---

## ローカル実行・デプロイ

### ローカル起動（価格自動取得つき・推奨）
```
npx wrangler pages dev . --port 8788 --compatibility-date=2026-04-27
```
表示された `http://localhost:8788` を開く。
- 静的サーバー（`python -m http.server`）では `/api/*` が動かず価格取得不可（手入力のみ）
- Finnhub（米株ほぼリアルタイム）をローカルで使う場合は `.dev.vars` に `FINNHUB_API_KEY` を設定（`.dev.vars.example` 参照）

### デプロイ（Cloudflare Pages）
- リポジトリを Cloudflare Pages に接続すれば `/functions` が自動でAPIになる（ビルド不要・出力ディレクトリはルート）
- 本番で Finnhub を使う場合は Pages の Settings > Environment variables に `FINNHUB_API_KEY` を設定

---

## 既知の制約（2026-05-30 時点）

- **投資信託（FUND）はツールから除外**（2026-05-30 すみぽん判断）。NAV を自動取得できる無料の汎用API（協会コード→基準価額）が無く、手入力では持つ意味がないため。市場は日本株(JP)・米国株(US)のみ。`MARKET_LABEL` 等に FUND 定義は後方互換で残るが UI では選択不可
- **日本株のファンダ（配当/PER/時価総額）**: ローカル wrangler では Yahoo quoteSummary がブロックされ取得不可。Cloudflare 本番デプロイ時に取得可能かは要検証（Notion バグ参照）
- **データ保存**: localStorage（ブラウザ単位）。マルチデバイス共有は Google スプレッドシート移行後（フェーズ1）
